apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-k8s-deployment.fullname" . }}
  labels:
    {{- include "my-k8s-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-k8s-deployment.selectorLabels" . | nindent 4 }}
